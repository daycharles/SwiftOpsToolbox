<UserControl x:Class="SwiftOpsToolbox.Views.MonthGridView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:local="clr-namespace:SwiftOpsToolbox.Views"
             mc:Ignorable="d"
             Height="Auto" Width="Auto">
    <UserControl.Resources>
        <local:StringToBrushConverter x:Key="StringToBrushConverter" />
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Month header - show current month and year -->
        <TextBlock Grid.Row="0"
                   Text="{Binding MonthTitle, RelativeSource={RelativeSource AncestorType=UserControl}}"
                   Foreground="#222"
                   FontSize="16"
                   FontWeight="SemiBold"
                   Margin="0,0,0,8" />

        <!-- Weekday headers -->
        <UniformGrid Grid.Row="1" Columns="7" Margin="0,8,0,8">
            <TextBlock Text="Sun" HorizontalAlignment="Center" FontWeight="Bold" />
            <TextBlock Text="Mon" HorizontalAlignment="Center" FontWeight="Bold" />
            <TextBlock Text="Tue" HorizontalAlignment="Center" FontWeight="Bold" />
            <TextBlock Text="Wed" HorizontalAlignment="Center" FontWeight="Bold" />
            <TextBlock Text="Thu" HorizontalAlignment="Center" FontWeight="Bold" />
            <TextBlock Text="Fri" HorizontalAlignment="Center" FontWeight="Bold" />
            <TextBlock Text="Sat" HorizontalAlignment="Center" FontWeight="Bold" />
        </UniformGrid>

        <!-- Days grid -->
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Disabled">
            <ItemsControl x:Name="DaysItems">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <UniformGrid Columns="7" />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border BorderBrush="#e6e6e6" BorderThickness="0.5" Background="White" Padding="8" MinHeight="80">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="*" />
                                </Grid.RowDefinitions>
                                <TextBlock Text="{Binding Day}" Foreground="#333" FontWeight="SemiBold" />
                                <ItemsControl Grid.Row="1" ItemsSource="{Binding Events}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Border CornerRadius="4" Padding="4" Margin="0,4,0,0" Background="{Binding Color, Converter={StaticResource StringToBrushConverter}}">
                                                <StackPanel Orientation="Horizontal">
                                                    <TextBlock Text="{Binding Time}" Foreground="White" FontSize="11" Margin="0,0,6,0" />
                                                    <TextBlock Text="{Binding Title}" Foreground="White" FontSize="12" TextTrimming="CharacterEllipsis" />
                                                </StackPanel>
                                            </Border>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>