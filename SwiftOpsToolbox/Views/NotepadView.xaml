<UserControl x:Class="SwiftOpsToolbox.Views.NotepadView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             Height="Auto" Width="Auto">

    <UserControl.InputBindings>
        <KeyBinding Key="N" Modifiers="Ctrl" Command="{Binding NewCommand}" />
        <KeyBinding Key="O" Modifiers="Ctrl" Command="{Binding OpenCommand}" />
        <KeyBinding Key="S" Modifiers="Ctrl" Command="{Binding SaveCommand}" />
        <KeyBinding Key="F" Modifiers="Ctrl" Command="{Binding FindCommand}" />
        <KeyBinding Key="H" Modifiers="Ctrl" Command="{Binding ReplaceCommand}" />
    </UserControl.InputBindings>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Toolbar replaced with fixed Grid to avoid ToolBar overflow behavior -->
        <Border Grid.Row="0" Background="#1A1A1A" Padding="6">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <!-- Left group: file actions and formatting -->
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Grid.Column="0">
                    <Button x:Name="BtnNew" Content="New" Width="60" Margin="2" Foreground="White" Background="Transparent" BorderBrush="#333" ToolTip="New file (Ctrl+N)" />
                    <Button x:Name="BtnOpen" Content="Open" Width="60" Margin="2" Foreground="White" Background="Transparent" BorderBrush="#333" ToolTip="Open file (Ctrl+O)" />
                    <Button x:Name="BtnSave" Content="Save" Width="60" Margin="2" Foreground="White" Background="Transparent" BorderBrush="#333" ToolTip="Save file (Ctrl+S)" />
                    <Button x:Name="BtnSaveAs" Content="Save As" Width="70" Margin="2" Foreground="White" Background="Transparent" BorderBrush="#333" ToolTip="Save As" />

                    <Rectangle Width="1" Height="24" Fill="#333" Margin="8,0" />

                    <Button x:Name="BtnUndo" Content="Undo" Width="60" Margin="2" Foreground="White" Background="Transparent" BorderBrush="#333" ToolTip="Undo (Ctrl+Z)" />
                    <Button x:Name="BtnRedo" Content="Redo" Width="60" Margin="2" Foreground="White" Background="Transparent" BorderBrush="#333" ToolTip="Redo (Ctrl+Y)" />

                    <Rectangle Width="1" Height="24" Fill="#333" Margin="8,0" />

                    <Button x:Name="BtnCut" Content="Cut" Width="50" Margin="2" Foreground="White" Background="Transparent" BorderBrush="#333" ToolTip="Cut (Ctrl+X)" />
                    <Button x:Name="BtnCopy" Content="Copy" Width="55" Margin="2" Foreground="White" Background="Transparent" BorderBrush="#333" ToolTip="Copy (Ctrl+C)" />
                    <Button x:Name="BtnPaste" Content="Paste" Width="55" Margin="2" Foreground="White" Background="Transparent" BorderBrush="#333" ToolTip="Paste (Ctrl+V)" />

                    <Rectangle Width="1" Height="24" Fill="#333" Margin="8,0" />

                    <Button x:Name="BtnFind" Content="Find" Width="50" Margin="2" Foreground="White" Background="Transparent" BorderBrush="#333" ToolTip="Find (Ctrl+F)" />
                    <Button x:Name="BtnReplace" Content="Replace" Width="70" Margin="2" Foreground="White" Background="Transparent" BorderBrush="#333" ToolTip="Replace (Ctrl+H)" />

                    <Rectangle Width="1" Height="24" Fill="#333" Margin="8,0" />

                    <ToggleButton x:Name="BtnBold" Content="B" FontWeight="Bold" Width="36" Margin="2" Foreground="White" Background="Transparent" BorderBrush="#333" ToolTip="Bold" />
                    <ToggleButton x:Name="BtnItalic" Content="I" FontStyle="Italic" Width="36" Margin="2" Foreground="White" Background="Transparent" BorderBrush="#333" ToolTip="Italic" />
                </StackPanel>

                <!-- center spacer (can hold search or status) -->
                <StackPanel Grid.Column="1" />

                <!-- Right group: preview toggle, popout, and font selectors -->
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Grid.Column="2">
                    <ToggleButton x:Name="BtnPreview" Content="Preview" Width="80" Margin="2" Foreground="White" Background="Transparent" BorderBrush="#333" />
                    <Button x:Name="BtnPopout" Content="Popout" Width="60" Margin="2" Foreground="White" Background="Transparent" BorderBrush="#333" ToolTip="Open preview in separate window" />

                    <ComboBox x:Name="FontFamilyCombo" Width="220" Margin="2" Foreground="White" Background="#202020">
                        <ComboBox.Resources>
                            <Style TargetType="TextBox">
                                <Setter Property="Background" Value="#202020" />
                                <Setter Property="Foreground" Value="White" />
                            </Style>
                        </ComboBox.Resources>
                    </ComboBox>

                    <ComboBox x:Name="FontSizeCombo" Width="80" Margin="2" IsEditable="True" Foreground="White" Background="#202020">
                        <ComboBox.Resources>
                            <Style TargetType="TextBox">
                                <Setter Property="Background" Value="#202020" />
                                <Setter Property="Foreground" Value="White" />
                            </Style>
                        </ComboBox.Resources>
                    </ComboBox>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Tabbed editor area with optional preview column -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="400" />
            </Grid.ColumnDefinitions>

            <TabControl x:Name="Tabs" Grid.Column="0">
                <TabItem Tag="">
                    <TabItem.Header>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Untitled" Foreground="White" Margin="0,0,8,0" />
                            <Button Content="x" Width="18" Height="18" Padding="0" Margin="0" Click="CloseTab_Click" ToolTip="Close tab" />
                        </StackPanel>
                    </TabItem.Header>
                    <RichTextBox x:Name="RtbSample" AcceptsTab="True" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" FontFamily="Consolas" FontSize="14" />
                </TabItem>
            </TabControl>

            <!-- Markdown preview pane -->
            <Border x:Name="PreviewBorder" Grid.Column="1" Background="#0F0F10" Padding="8" Visibility="Collapsed">
                <WebBrowser x:Name="MarkdownPreview" />
            </Border>
        </Grid>
    </Grid>
</UserControl>