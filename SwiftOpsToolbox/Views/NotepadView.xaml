<UserControl x:Class="SwiftOpsToolbox.Views.NotepadView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             Height="Auto" Width="Auto">

    <UserControl.InputBindings>
        <KeyBinding Key="N" Modifiers="Ctrl" Command="{Binding NewCommand}" />
        <KeyBinding Key="O" Modifiers="Ctrl" Command="{Binding OpenCommand}" />
        <KeyBinding Key="S" Modifiers="Ctrl" Command="{Binding SaveCommand}" />
        <KeyBinding Key="F" Modifiers="Ctrl" Command="{Binding FindCommand}" />
        <KeyBinding Key="H" Modifiers="Ctrl" Command="{Binding ReplaceCommand}" />
    </UserControl.InputBindings>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Toolbar replaced with fixed Grid to avoid ToolBar overflow behavior -->
        <Border Grid.Row="0" Background="#1A1A1A" Padding="6">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <!-- Left group: file actions and formatting -->
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Grid.Column="0">
                    <Button x:Name="BtnNew" Width="50" Height="50" Margin="2" Foreground="White" Background="Transparent" BorderBrush="#333" ToolTip="New file (Ctrl+N)">
                        <StackPanel Orientation="Vertical" HorizontalAlignment="Center">
                            <TextBlock FontFamily="Segoe UI Emoji" FontSize="18" Text="??" Foreground="White" HorizontalAlignment="Center" />
                            <TextBlock Text="New" Foreground="White" Opacity="0.95" FontSize="8" HorizontalAlignment="Center" Margin="0,2,0,0" />
                        </StackPanel>
                    </Button>
                    <Button x:Name="BtnOpen" Width="50" Height="50" Margin="2" Foreground="White" Background="Transparent" BorderBrush="#333" ToolTip="Open file (Ctrl+O)">
                        <StackPanel Orientation="Vertical" HorizontalAlignment="Center">
                            <TextBlock FontFamily="Segoe UI Emoji" FontSize="18" Text="??" Foreground="White" HorizontalAlignment="Center" />
                            <TextBlock Text="Open" Foreground="White" Opacity="0.95" FontSize="8" HorizontalAlignment="Center" Margin="0,2,0,0" />
                        </StackPanel>
                    </Button>
                    <Button x:Name="BtnSave" Width="50" Height="50" Margin="2" Foreground="White" Background="Transparent" BorderBrush="#333" ToolTip="Save file (Ctrl+S)">
                        <StackPanel Orientation="Vertical" HorizontalAlignment="Center">
                            <TextBlock FontFamily="Segoe UI Emoji" FontSize="18" Text="??" Foreground="White" HorizontalAlignment="Center" />
                            <TextBlock Text="Save" Foreground="White" Opacity="0.95" FontSize="8" HorizontalAlignment="Center" Margin="0,2,0,0" />
                        </StackPanel>
                    </Button>
                    <Button x:Name="BtnSaveAs" Width="50" Height="50" Margin="2" Foreground="White" Background="Transparent" BorderBrush="#333" ToolTip="Save As">
                        <StackPanel Orientation="Vertical" HorizontalAlignment="Center">
                            <TextBlock FontFamily="Segoe UI Emoji" FontSize="18" Text="??" Foreground="White" HorizontalAlignment="Center" />
                            <TextBlock Text="Save As" Foreground="White" Opacity="0.95" FontSize="7" HorizontalAlignment="Center" Margin="0,2,0,0" />
                        </StackPanel>
                    </Button>

                    <Rectangle Width="1" Height="24" Fill="#333" Margin="8,0" />

                    <Button x:Name="BtnUndo" Width="50" Height="50" Margin="2" Foreground="White" Background="Transparent" BorderBrush="#333" ToolTip="Undo (Ctrl+Z)">
                        <StackPanel Orientation="Vertical" HorizontalAlignment="Center">
                            <TextBlock FontFamily="Segoe UI Emoji" FontSize="18" Text="?" Foreground="White" HorizontalAlignment="Center" />
                            <TextBlock Text="Undo" Foreground="White" Opacity="0.95" FontSize="8" HorizontalAlignment="Center" Margin="0,2,0,0" />
                        </StackPanel>
                    </Button>
                    <Button x:Name="BtnRedo" Width="50" Height="50" Margin="2" Foreground="White" Background="Transparent" BorderBrush="#333" ToolTip="Redo (Ctrl+Y)">
                        <StackPanel Orientation="Vertical" HorizontalAlignment="Center">
                            <TextBlock FontFamily="Segoe UI Emoji" FontSize="18" Text="?" Foreground="White" HorizontalAlignment="Center" />
                            <TextBlock Text="Redo" Foreground="White" Opacity="0.95" FontSize="8" HorizontalAlignment="Center" Margin="0,2,0,0" />
                        </StackPanel>
                    </Button>

                    <Rectangle Width="1" Height="24" Fill="#333" Margin="8,0" />

                    <Button x:Name="BtnCut" Width="50" Height="50" Margin="2" Foreground="White" Background="Transparent" BorderBrush="#333" ToolTip="Cut (Ctrl+X)">
                        <StackPanel Orientation="Vertical" HorizontalAlignment="Center">
                            <TextBlock FontFamily="Segoe UI Emoji" FontSize="18" Text="??" Foreground="White" HorizontalAlignment="Center" />
                            <TextBlock Text="Cut" Foreground="White" Opacity="0.95" FontSize="8" HorizontalAlignment="Center" Margin="0,2,0,0" />
                        </StackPanel>
                    </Button>
                    <Button x:Name="BtnCopy" Width="50" Height="50" Margin="2" Foreground="White" Background="Transparent" BorderBrush="#333" ToolTip="Copy (Ctrl+C)">
                        <StackPanel Orientation="Vertical" HorizontalAlignment="Center">
                            <TextBlock FontFamily="Segoe UI Emoji" FontSize="18" Text="??" Foreground="White" HorizontalAlignment="Center" />
                            <TextBlock Text="Copy" Foreground="White" Opacity="0.95" FontSize="8" HorizontalAlignment="Center" Margin="0,2,0,0" />
                        </StackPanel>
                    </Button>
                    <Button x:Name="BtnPaste" Width="50" Height="50" Margin="2" Foreground="White" Background="Transparent" BorderBrush="#333" ToolTip="Paste (Ctrl+V)">
                        <StackPanel Orientation="Vertical" HorizontalAlignment="Center">
                            <TextBlock FontFamily="Segoe UI Emoji" FontSize="18" Text="??" Foreground="White" HorizontalAlignment="Center" />
                            <TextBlock Text="Paste" Foreground="White" Opacity="0.95" FontSize="8" HorizontalAlignment="Center" Margin="0,2,0,0" />
                        </StackPanel>
                    </Button>

                    <Rectangle Width="1" Height="24" Fill="#333" Margin="8,0" />

                    <Button x:Name="BtnFind" Width="50" Height="50" Margin="2" Foreground="White" Background="Transparent" BorderBrush="#333" ToolTip="Find (Ctrl+F)">
                        <StackPanel Orientation="Vertical" HorizontalAlignment="Center">
                            <TextBlock FontFamily="Segoe UI Emoji" FontSize="18" Text="??" Foreground="White" HorizontalAlignment="Center" />
                            <TextBlock Text="Find" Foreground="White" Opacity="0.95" FontSize="8" HorizontalAlignment="Center" Margin="0,2,0,0" />
                        </StackPanel>
                    </Button>
                    <Button x:Name="BtnReplace" Width="50" Height="50" Margin="2" Foreground="White" Background="Transparent" BorderBrush="#333" ToolTip="Replace (Ctrl+H)">
                        <StackPanel Orientation="Vertical" HorizontalAlignment="Center">
                            <TextBlock FontFamily="Segoe UI Emoji" FontSize="18" Text="??" Foreground="White" HorizontalAlignment="Center" />
                            <TextBlock Text="Replace" Foreground="White" Opacity="0.95" FontSize="7" HorizontalAlignment="Center" Margin="0,2,0,0" />
                        </StackPanel>
                    </Button>

                    <Rectangle Width="1" Height="24" Fill="#333" Margin="8,0" />

                    <ToggleButton x:Name="BtnBold" Width="50" Height="50" Margin="2" Foreground="White" Background="Transparent" BorderBrush="#333" ToolTip="Bold">
                        <StackPanel Orientation="Vertical" HorizontalAlignment="Center">
                            <TextBlock FontSize="18" Text="B" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center" />
                            <TextBlock Text="Bold" FontWeight="Bold" Foreground="White" Opacity="0.95" FontSize="8" HorizontalAlignment="Center" Margin="0,2,0,0" />
                        </StackPanel>
                    </ToggleButton>
                    <ToggleButton x:Name="BtnItalic" Width="50" Height="50" Margin="2" Foreground="White" Background="Transparent" BorderBrush="#333" ToolTip="Italic">
                        <StackPanel Orientation="Vertical" HorizontalAlignment="Center">
                            <TextBlock FontSize="18" Text="I" FontStyle="Italic" Foreground="White" HorizontalAlignment="Center" />
                            <TextBlock Text="Italic" FontStyle="Italic" Foreground="White" Opacity="0.95" FontSize="8" HorizontalAlignment="Center" Margin="0,2,0,0" />
                        </StackPanel>
                    </ToggleButton>
                </StackPanel>

                <!-- center spacer (can hold search or status) -->
                <StackPanel Grid.Column="1" />

                <!-- Right group: preview toggle, popout, and font selectors -->
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Grid.Column="2">
                    <ToggleButton x:Name="BtnPreview" Width="60" Height="50" Margin="2" Foreground="White" Background="Transparent" BorderBrush="#333">
                        <StackPanel Orientation="Vertical" HorizontalAlignment="Center">
                            <TextBlock FontFamily="Segoe UI Emoji" FontSize="18" Text="???" Foreground="White" HorizontalAlignment="Center" />
                            <TextBlock Text="Preview" Foreground="White" Opacity="0.95" FontSize="8" HorizontalAlignment="Center" Margin="0,2,0,0" />
                        </StackPanel>
                    </ToggleButton>
                    <Button x:Name="BtnPopout" Width="60" Height="50" Margin="2" Foreground="White" Background="Transparent" BorderBrush="#333" ToolTip="Open preview in separate window">
                        <StackPanel Orientation="Vertical" HorizontalAlignment="Center">
                            <TextBlock FontFamily="Segoe UI Emoji" FontSize="18" Text="?" Foreground="White" HorizontalAlignment="Center" />
                            <TextBlock Text="Popout" Foreground="White" Opacity="0.95" FontSize="8" HorizontalAlignment="Center" Margin="0,2,0,0" />
                        </StackPanel>
                    </Button>

                    <ComboBox x:Name="FontFamilyCombo" Width="220" Margin="2" Foreground="Black" Background="#202020" />

                    <ComboBox x:Name="FontSizeCombo" Width="80" Margin="2" IsEditable="True" Foreground="Black" Background="#202020" />
                </StackPanel>
            </Grid>
        </Border>

        <!-- Tabbed editor area with optional preview column -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="400" />
            </Grid.ColumnDefinitions>

            <TabControl x:Name="Tabs" Grid.Column="0">
                <TabItem Tag="">
                    <TabItem.Header>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Untitled" Foreground="White" Margin="0,0,8,0" />
                            <Button Content="x" Width="18" Height="18" Padding="0" Margin="0" Click="CloseTab_Click" ToolTip="Close tab" />
                        </StackPanel>
                    </TabItem.Header>
                    <RichTextBox x:Name="RtbSample" AcceptsTab="True" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" FontFamily="Consolas" FontSize="14" />
                </TabItem>
            </TabControl>

            <!-- Markdown preview pane -->
            <Border x:Name="PreviewBorder" Grid.Column="1" Background="#0F0F10" Padding="8" Visibility="Collapsed">
                <WebBrowser x:Name="MarkdownPreview" />
            </Border>
        </Grid>
    </Grid>
</UserControl>