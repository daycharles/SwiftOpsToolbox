<UserControl x:Class="SwiftOpsToolbox.Views.FileSearchView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
             mc:Ignorable="d"
             Height="Auto" Width="Auto">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis" />
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Top search bar -->
        <DockPanel Grid.Row="0" Margin="8">
            <TextBox x:Name="SearchBox" Width="600" Height="28" VerticalAlignment="Center" Margin="0" FontSize="13" Padding="6"
                     TextChanged="SearchBox_TextChanged" />
            <StackPanel Orientation="Horizontal" DockPanel.Dock="Right">
                <Button x:Name="BtnOpenLog" Width="80" Height="50" Margin="8,0,0,0">
                    <StackPanel Orientation="Vertical" HorizontalAlignment="Center">
                        <TextBlock FontFamily="Segoe MDL2 Assets" Text="?" FontSize="14" Foreground="White" HorizontalAlignment="Center" />
                        <TextBlock Text="Open Log" Foreground="White" Opacity="0.95" FontSize="8" HorizontalAlignment="Center" Margin="0,2,0,0" />
                    </StackPanel>
                </Button>
                <Button x:Name="BtnSftpToggle" Width="70" Height="50" Margin="8,0,0,0">
                    <StackPanel Orientation="Vertical" HorizontalAlignment="Center">
                        <TextBlock FontFamily="Segoe MDL2 Assets" Text="?" FontSize="14" Foreground="White" HorizontalAlignment="Center" />
                        <TextBlock Text="SFTP" Foreground="White" Opacity="0.95" FontSize="8" HorizontalAlignment="Center" Margin="0,2,0,0" />
                    </StackPanel>
                </Button>
            </StackPanel>
        </DockPanel>

        <!-- SFTP connection panel -->
        <Border Grid.Row="1" Margin="8" Padding="8" Background="#0F0F10" CornerRadius="6" BorderBrush="#222" BorderThickness="1" Visibility="Collapsed" x:Name="SftpPanel">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <ComboBox x:Name="CbProfiles" Width="200" Height="26" Margin="0,0,8,0" DisplayMemberPath="Name" ItemsSource="{Binding SftpService.Profiles}" />
                <Button x:Name="BtnProfileEdit" Width="60" Height="45" Margin="0,0,8,0">
                    <StackPanel Orientation="Vertical" HorizontalAlignment="Center">
                        <TextBlock FontFamily="Segoe MDL2 Assets" Text="?" FontSize="12" Foreground="White" HorizontalAlignment="Center" />
                        <TextBlock Text="Edit" Foreground="White" Opacity="0.95" FontSize="8" HorizontalAlignment="Center" Margin="0,2,0,0" />
                    </StackPanel>
                </Button>
                <TextBox x:Name="SftpHost" Width="150" Height="26" Margin="0,0,8,0" mah:TextBoxHelper.Watermark="host" />
                <TextBox x:Name="SftpPort" Width="60" Height="26" Margin="0,0,8,0" Text="22" />
                <TextBox x:Name="SftpUser" Width="120" Height="26" Margin="0,0,8,0" mah:TextBoxHelper.Watermark="username" />
                <PasswordBox x:Name="SftpPass" Width="150" Height="26" Margin="0,0,8,0" ToolTip="password" />
                <Button x:Name="BtnSftpConnect" Width="80" Height="45">
                    <StackPanel Orientation="Vertical" HorizontalAlignment="Center">
                        <TextBlock FontFamily="Segoe MDL2 Assets" Text="?" FontSize="12" Foreground="White" HorizontalAlignment="Center" />
                        <TextBlock Text="Connect" Foreground="White" Opacity="0.95" FontSize="8" HorizontalAlignment="Center" Margin="0,2,0,0" />
                    </StackPanel>
                </Button>
                <Button x:Name="BtnSftpDisconnect" Width="80" Height="45" Margin="8,0,0,0">
                    <StackPanel Orientation="Vertical" HorizontalAlignment="Center">
                        <TextBlock FontFamily="Segoe MDL2 Assets" Text="?" FontSize="12" Foreground="White" HorizontalAlignment="Center" />
                        <TextBlock Text="Disconnect" Foreground="White" Opacity="0.95" FontSize="7" HorizontalAlignment="Center" Margin="0,2,0,0" />
                    </StackPanel>
                </Button>
            </StackPanel>
        </Border>

        <!-- Status line -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="8" VerticalAlignment="Center">
            <TextBlock x:Name="IndexStatus" Foreground="White" Margin="0,0,12,0" Text="Idle" />
            <TextBlock x:Name="CountText" Foreground="White" />
            <ProgressBar x:Name="IndexProgress" Width="180" Height="12" Margin="12,0,0,0" IsIndeterminate="True" Visibility="{Binding IsIndexing, Converter={StaticResource BoolToVis}}" />
        </StackPanel>

        <!-- Results list - enable virtualization for large datasets -->
        <Grid Grid.Row="3" Margin="8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="400" />
            </Grid.ColumnDefinitions>

            <ListView x:Name="ResultsList" Grid.Column="0" ItemsSource="{Binding FileResults}"
                      ScrollViewer.IsDeferredScrollingEnabled="True">
                <ListView.ItemsPanel>
                    <ItemsPanelTemplate>
                        <VirtualizingStackPanel VirtualizationMode="Recycling" />
                    </ItemsPanelTemplate>
                </ListView.ItemsPanel>
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="Name" DisplayMemberBinding="{Binding Name}" Width="300" />
                        <GridViewColumn Header="Path" DisplayMemberBinding="{Binding Path}" Width="600" />
                        <GridViewColumn Header="Size" DisplayMemberBinding="{Binding SizeBytes}" Width="100" />
                        <GridViewColumn Header="Modified" DisplayMemberBinding="{Binding Modified}" Width="160" />
                    </GridView>
                </ListView.View>
            </ListView>

            <!-- Remote files list -->
            <Border Grid.Column="1" Margin="12,0,0,0" Background="#0F0F10" CornerRadius="6" BorderBrush="#222" BorderThickness="1">
                <StackPanel>
                    <TextBlock Text="SFTP Remote" Foreground="White" FontSize="14" FontWeight="SemiBold" Margin="8" />
                    <ListView x:Name="RemoteList" ItemsSource="{Binding SftpService.Files}" Height="400" Margin="8">
                        <ListView.View>
                            <GridView>
                                <GridViewColumn Header="Name" DisplayMemberBinding="{Binding Name}" Width="200" />
                                <GridViewColumn Header="Path" DisplayMemberBinding="{Binding Path}" Width="180" />
                                <GridViewColumn Header="Size" DisplayMemberBinding="{Binding SizeBytes}" Width="80" />
                                <GridViewColumn Header="Modified" DisplayMemberBinding="{Binding Modified}" Width="120" />
                            </GridView>
                        </ListView.View>
                    </ListView>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>